services:
  - type: web
    name: whatsapp-chatbot-service
    env: docker
    plan: free
    region: oregon
    # Render injects the PORT environment variable.
    # The Docker container will use it as specified in application.properties or entrypoint.
